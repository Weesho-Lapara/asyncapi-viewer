# Plain Python-Markdown page

This page is rendered by `render.py` with plain Python-Markdown (no MkDocs) and opened by
`markdown.spec.ts`. Both example documents, one per syntax, plus a missing one.

<asyncapi-viewer src="../../../../docs/examples/orders-v3.yaml" sidebar></asyncapi-viewer>

Some prose between the viewers.

```asyncapi
src: ../../../../docs/examples/accounts-v2.json
messageExamples: false
```

<asyncapi-viewer src="../../../../docs/examples/nope.yaml"></asyncapi-viewer>
